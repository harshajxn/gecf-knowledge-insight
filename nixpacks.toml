[phases.setup]
# Installs the C compiler and SWIG, which are required to build PyMuPDF
aptPacks = ["build-essential", "swig"]

[phases.install]
# Best practice: upgrade pip first, then install requirements
cmds = ["pip install --upgrade pip setuptools wheel", "pip install -r requirements.txt"]